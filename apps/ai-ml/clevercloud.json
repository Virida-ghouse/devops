{
  "build": {
    "type": "python",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "command": "gunicorn app:app --bind 0.0.0.0:$PORT"
  },
  "environment": {
    "PYTHON_VERSION": "3.11"
  }
}
